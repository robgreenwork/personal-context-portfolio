{
  "name": "multi-workspace-notion-mcp",
  "version": "1.0.0",
  "description": "MCP server that connects to multiple Notion workspaces (personal + Bridgit)",
  "type": "module",
  "main": "notion-mcp-server.js",
  "scripts": {
    "start": "node notion-mcp-server.js",
    "dev": "node --watch notion-mcp-server.js"
  },
  "dependencies": {
    "@notionhq/client": "^2.2.15",
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
